export function getTotalPullsPerPityAndRarityKvKey(poolId: number) {
  return `${poolId}-total-pulls-per-pity-and-rarity`
}

export function getTotalPullsByItemPerDayKvKey(poolId: number) {
  return `${poolId}-total-pulls-by-item-per-day`
}
